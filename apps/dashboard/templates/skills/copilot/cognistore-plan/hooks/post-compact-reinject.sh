#!/usr/bin/env bash
cat <<'EOF'
{
  "systemMessage": "📋 Context compacted — reload plan state:\n1. listPlanTasks(planId) to get current task statuses\n2. Continue tracking from your in_progress task"
}
EOF
