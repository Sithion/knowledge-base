{
  "name": "@ai-knowledge/core",
  "version": "0.9.7",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc && cp -r src/db/migrations dist/db/migrations && cp -r src/db/seeds dist/db/seeds",
    "clean": "rm -rf dist",
    "test": "if find . -name '*.test.ts' -o -name '*.spec.ts' | grep -q .; then npx playwright test; else echo 'No tests found, skipping'; fi",
    "db:generate": "pnpm build && drizzle-kit generate",
    "db:push": "pnpm build && drizzle-kit push",
    "db:migrate": "drizzle-kit migrate"
  },
  "dependencies": {
    "@ai-knowledge/shared": "workspace:*",
    "better-sqlite3": "^12.8.0",
    "drizzle-orm": "^0.38.0",
    "sqlite-vec": "^0.1.7",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/better-sqlite3": "^7.6.13",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0"
  }
}
